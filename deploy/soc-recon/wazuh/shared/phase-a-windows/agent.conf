<!--
  Shared group config pushed from the manager to every agent in `phase-a-windows`.

  Kept in git deliberately: /var/ossec/etc/shared/ lives inside the wazuh_etc Docker
  volume, so a `docker compose down -v` erases it. Same failure class as the ISM
  retention policy lost in the Phase 0.5 rebuild (see wazuh/ism/README.md).

  Agents pull this automatically within ~5 min of a change, or immediately on restart.
  Windows default channels (Security, System, Application) are already collected by the
  agent's own ossec.conf — this adds the Sysmon channel on top.

  ── ONE-TIME CHOWN REQUIRED AFTER ANY `down -v` ──
  The entrypoint's sync CREATES this group directory as root:root 755. wazuh-remoted runs
  as `wazuh` and needs write access to generate merged.mg — the file agents actually pull.
  Without it an agent enrolls into the group cleanly and silently never receives this
  config. Verified 2026-07-25: the manager's own `default` group is wazuh:wazuh 770.

      docker compose exec wazuh.manager \
        chown -R wazuh:wazuh /var/ossec/etc/shared/phase-a-windows
      docker compose exec wazuh.manager \
        chmod 770 /var/ossec/etc/shared/phase-a-windows

  Only needed once per volume lifetime — the entrypoint leaves an existing directory's
  ownership alone (confirmed across a manager restart), so this does NOT recur on reboot.
-->
<agent_config>

  <!-- Sysmon (Olaf Hartong sysmon-modular). This is the Phase A.1 deliverable:
       proves the Sysmon -> eventchannel -> agent -> manager -> indexer chain, and
       is the telemetry Phase B's Sigma rules are written against. -->
  <localfile>
    <location>Microsoft-Windows-Sysmon/Operational</location>
    <log_format>eventchannel</log_format>
  </localfile>

</agent_config>
